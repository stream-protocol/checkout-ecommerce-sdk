declare type SupportedSplTokens = "dust" | "str" |"samo" | "shdw";
declare type CheckoutItem = {
    name: string;
    price: number;
    image: string;
    quantity: number;
    size?: string;
};
declare type CheckoutConfig = {
    collect_shipping_address: boolean;
};

export { CheckoutConfig, CheckoutItem, SupportedSplTokens };
