import { ReactNode } from 'react';

interface IProps {
    handleSession: () => Promise<any>;
    children?: ReactNode;
}
interface IProviderProps {
    children: ReactNode;
    publicApiKey: string;
}

export { IProps, IProviderProps };
