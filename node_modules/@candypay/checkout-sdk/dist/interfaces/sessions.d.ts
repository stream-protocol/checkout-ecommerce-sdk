import { CheckoutItem, SupportedSplTokens } from '../types/index.js';

interface CreateSessionOptions {
    items: CheckoutItem[];
    tokens?: SupportedSplTokens[];
    success_url: string;
    cancel_url: string;
    shipping_fees?: number;
}
interface CreateSessionResponse {
    message: string;
    session_id: string;
    order_id: string;
}

export { CreateSessionOptions, CreateSessionResponse };
